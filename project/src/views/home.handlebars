<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home - Listado de Productos</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <header>
        <h1>Listado de Productos</h1>
    </header>
    <main>
        <section class="products-list">
            {{#each products}}
            <div class="product-card">
                <h3>{{title}}</h3>
                <p>{{description}}</p>
                <p class="price">${{price}}</p>
                <p><strong>Categoría:</strong> {{category}}</p>
                <button class="add-to-cart-btn" data-id="{{id}}">Añadir al carrito</button>
            </div>
            {{/each}}
        </section>
    </main>
</body>
</html>
